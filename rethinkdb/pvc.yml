apiVersion: v1

kind: PersistentVolumeClaim

metadata:

  name: rethinkdb-pvc

  namespace: rethinkdb-server

spec:

  accessModes:
    - ReadWriteOnce

  storageClassName: local-path

  resources:

    requests:
      storage: 5Gi

